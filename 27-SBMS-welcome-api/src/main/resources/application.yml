server:
  port: 8080
spring:
  application:
    name: WELCOME-API
  boot:
    admin:
      client:
        url: http://localhost:2222/
eureka:
  client:
    service-url:
     defaultZone: http://localhost:8761/eureka/
      
      
management:
    endpoints:
      web:
        exposure:
          include: '*'